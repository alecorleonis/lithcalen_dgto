<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @list.user.username %>
</p>

<p>
  <strong>List name:</strong>
  <%= @list.list_name %>
</p>

<% @l = List.find_by_id(@list.id) %>
<% @us = User.find_by_id(@l.user_id).id %>
<%= link_to 'Edit', edit_list_path(:id => @list.id, :user => @us) %> 
<%= link_to 'Back', lists_path(:user => @l.user_id) %>
