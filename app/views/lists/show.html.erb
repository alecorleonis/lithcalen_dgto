<p id="notice"><%= notice %></p>
<ul>
<p>
	<li>
  <strong>Usuario:</strong>
  <%= @list.user.username %>
	</li>
</p>

<p>
	<li>
  <strong>Lista:</strong>
  <%= @list.list_name %>
	</li>
</p>
</ul>

<% @l = List.find_by_id(@list.id) %>
<% @us = User.find_by_id(@l.user_id).id %>
<%= link_to 'Editar', edit_list_path(:id => @list.id, :user => @us) %> |
<%= link_to 'Regresar', lists_path(:user => @l.user_id) %>
