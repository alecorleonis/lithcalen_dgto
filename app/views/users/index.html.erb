<p id="notice"><%= notice %></p>

<h1>Listing Users</h1> 

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.where.not(id: current_user.id).each do |user| %>
      <tr>
	<td><%= link_to user.username, {:controller => :lists, :action=>'index', :user => user.id}%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Back', lists_path(:user => current_user.id) %>
